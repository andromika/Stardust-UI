/**
 * Reference value for the Storybook sidebar category.
 * Storybook v10+ requires literal string titles in meta (no template literals),
 * so stories use 'Stardust UI/...' directly. When renaming the category,
 * find-replace 'Stardust UI' in stardust-ui/**/*.stories.ts titles.
 */
export const STORYBOOK_CATEGORY = 'Stardust UI';
