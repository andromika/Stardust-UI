name: Archived Sync UI to PolestarLabs

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sync-main-repo:
    name: Archived sync to PolestarLabs/Stardust-UI
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow notice
        run: |
          echo "sync-to-polestarlabs.yml is archived."
          echo "Use .github/workflows/mirror-to-dashboard.yml to sync Stardust UI changes into PolestarLabs/dashboard."
